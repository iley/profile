#!/bin/bash

server=$1
table=$2

for scale in '' /daily /weekly /monthly
do
	ssh statbox@$server "
		yes yes | sctl xreport-delete $table$scale all
		pushd /var/lib/statbox-vcfs/slave-root; git pull; popd
	"
done
